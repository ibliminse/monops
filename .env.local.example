# ============================================
# MonOps Environment Variables
# Copy to .env.local and fill in your values
# ============================================

# --- Required ---

# Monad Mainnet RPC (public endpoint works, private is faster)
NEXT_PUBLIC_MONAD_RPC_URL=https://rpc.monad.xyz
NEXT_PUBLIC_MONAD_WS_URL=wss://rpc.monad.xyz

# WalletConnect Project ID (required for RainbowKit wallet connection)
# Get one at https://cloud.walletconnect.com
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=your_project_id_here

# --- API Keys (optional, enable richer data) ---

# Moralis API key — primary source for NFT and ERC-20 token data
# Without this, falls back to Etherscan V2 (less complete)
# Get one at https://moralis.io
MORALIS_API_KEY=

# Etherscan V2 API key — fallback data source for NFTs and tokens
# Used when Moralis is unavailable or not configured
# Get one at https://etherscan.io/apis
NEXT_PUBLIC_MONADSCAN_API_KEY=

# --- Analytics (optional) ---

# Google Analytics measurement ID (only loads after user consent)
NEXT_PUBLIC_GA_MEASUREMENT_ID=

# --- Tuning (optional) ---

# Block range for log-based inventory scanning (default: 500000)
# Increase for older collections, decrease for faster scans
NEXT_PUBLIC_SCAN_BLOCK_RANGE=500000
